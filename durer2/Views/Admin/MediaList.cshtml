@model IList<durer2.ServiceReference.ContentDto>

@{
    ViewBag.Title = "Basın";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-content">
    <div class="row">
        <div class="col-md-2">
            <div class="sidebar content-box" style="display: block;">
                <ul class="nav">
                    <li><a href="/Admin/Pages/28"><i class="glyphicon glyphicon-file"></i> Sayfa Yönetimi</a></li>
                    <li><a href="/Admin/News"><i class="glyphicon glyphicon-bullhorn"></i> Haber Yönetimi</a></li>
                    
                    <li class="current"><a href="/Admin/MediaList"><i class="glyphicon glyphicon-globe"></i> Basın</a></li>
                    <li><a href="/Admin/Catalogs"><i class="glyphicon glyphicon-certificate"></i> Kataloglar</a></li>
                </ul>
            </div>
        </div>
        <div class="col-md-10">
            <div class="row">
                <div class="col-md-8">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="content-box-header">
                                <div class="panel-title">Basın</div>
                                <div class="panel-options">
                                    <a href="/Admin/BannerList/1" class="btn btn-default btn-xs"><i class="glyphicon glyphicon-pencil"></i> Sayfa Görselini Düzenle</a>
                                    <a href="/Admin/MediaAdd" class="btn btn-default btn-xs"><i class="glyphicon glyphicon-plus"></i> Ekle</a>
                                </div>
                            </div>
                            <div class="content-box-large box-with-header">
                                @if (Model.Count > 0)
                                {
                                    <table class="table table-striped">
                                        <thead>
                                            <tr>
                                                <th>Başlık</th>
                                                <th>Düzenleme</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @foreach (var item in Model)
                                            {
                                                <tr>
                                                    <td>
                                                        @((item.Title.Length > 50) ? item.Title.Substring(0, 50) + "..." : item.Title)
                                                    </td>
                                                    <td>

                                                        @if (item.Active)
                                                        {
                                                            <a href="/Admin/MediaActive/@item.Id" class="btn btn-success btn-xs">
                                                                Pasifleştir
                                                            </a>
                                                        }
                                                        else
                                                        {
                                                            <a href="/Admin/MediaActive/@item.Id" class="btn btn-danger btn-xs">
                                                                Aktifleştir
                                                            </a>
                                                        }
                                                        <a href="/Admin/MediaOrder/@item.Id?isDown=false" class="btn btn-default btn-xs">
                                                            <span class="glyphicon glyphicon-arrow-up"></span>
                                                        </a>
                                                        <a href="/Admin/MediaOrder/@item.Id?isDown=true" class="btn btn-default btn-xs">
                                                            <span class="glyphicon glyphicon-arrow-down"></span>
                                                        </a>
                                                        <a href="#" class="btn btn-default btn-xs" onclick="javascript:confirm('Emin misiniz?')?document.location = '/Admin/MediaDelete/@item.Id':'';">
                                                            <span class="glyphicon glyphicon-remove"></span> Sil
                                                        </a>
                                                        <a href="/Admin/Media/@item.Id" class="btn btn-default btn-xs"><i class="glyphicon glyphicon-edit"></i> Düzenle</a>
                                                    </td>
                                                </tr>
                                            }
                                        </tbody>
                                    </table>
                                }
                                else
                                {
                                    <span>Henüz birşey eklenmemiş.</span>
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
