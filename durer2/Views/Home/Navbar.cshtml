    <ul>
        <li>
            <a href="javascript: void(0);">@durer2.Resource.Resources.CompanyUpper</a>
            <ul>
                @foreach (var item in ViewBag.Nav1)
            {
                    <li><a href="/@item.Permalink">@item.Title</a></li>
                }
                <li><a href="@durer2.Resource.Resources.NewsUrl">@durer2.Resource.Resources.NewsUpper</a></li>
                <li><a href="@durer2.Resource.Resources.PressUrl">@durer2.Resource.Resources.PressUpper</a></li>
            </ul>
        </li>
        <li>
            <a href="javascript: void(0);">@durer2.Resource.Resources.IndustriesUpper</a>
            <ul>
                @foreach (var item in ViewBag.Nav2)
                {
                    <li><a href="/@item.Permalink">@item.Title</a></li>
                }
            </ul>
        </li>
        <li>
            <a href="javascript: void(0);">@durer2.Resource.Resources.ProductsUpper</a>
            <ul>
                @foreach (var item in ViewBag.Nav3)
                {
                    <li><a href="/@item.Permalink">@item.Title</a></li>
                }
                <li><a href="@durer2.Resource.Resources.CatalogUrl">@durer2.Resource.Resources.CatalogUpper</a></li>
            </ul>
        </li>
        <li>
            <a href="@durer2.Resource.Resources.ServicesUrl">@durer2.Resource.Resources.OurServicesUpper</a>
            <ul>
                @foreach (var item in ViewBag.Nav4)
            {
                    <li><a href="/@item.Permalink">@item.Title</a></li>
                }
            </ul>
        </li>
        <li>
            <a href="javascript: void(0);">@durer2.Resource.Resources.HrUpper</a>
            <ul>
                @foreach (var item in ViewBag.Nav6)
            {
                    <li><a href="/@item.Permalink">@item.Title</a></li>
                }
            </ul>
        </li>
        <li><a href="@durer2.Resource.Resources.ContactUrl">@durer2.Resource.Resources.ContactUpper</a></li>
    </ul>
